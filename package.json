{
  "name": "whitehouse-rocket-run",
  "version": "2.0.0",
  "private": true,
  "description": "A horizontal, one-button arcade runner with Trump and Elon riding one rocket.",
  "scripts": {
    "dev": "node scripts/serve.cjs",
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": { "node": ">=18" }
}
